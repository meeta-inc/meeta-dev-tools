# excel-to-md 환경변수 예시 — 값은 비워둔다(C-9: 평문 비밀 금지).
# 실제 값은 .env 에 채우고 .env 는 절대 커밋하지 않는다.

# LLM 서술(c) 활성화용 Anthropic API 키. 미설정 시 LLM 분석은 graceful skip.
ANTHROPIC_API_KEY=

# (선택) 사용할 LLM 모델명. 미설정 시 기본값(claude-haiku-4-5) 사용.
EXCEL_TO_MD_LLM_MODEL=
